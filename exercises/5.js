 /**
 * Make a function that takes an array of numbers and a number.
 * That function return that array where all elements smaller than
 * second parameter are filtered out.
 **/

const solution = () => {
  // answer here
};

module.exports = {
  solution,
};